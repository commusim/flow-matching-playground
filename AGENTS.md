# AGENTS.md — 科学图像生成学习项目规范

## 项目目标

本项目是一个以“先理解原理、再逐步扩展”为目标的学习项目，当前路线为：二维数据拟合、二维条件泛化、最优传输与Flow Matching、简单图像生成，之后再连接科学图像质量评价与生成模型研究。项目优先保证原理清晰、实验可复现、可视化充分和模块低耦合，不以一开始实现复杂生产系统为目标。

## 强制目录结构（L4）

所有新学习模块都应按以下结构组织：

```text
项目根目录/
├── src/
│   ├── utils/       # 公共基础能力与可复用工具
│   ├── modules/     # 独立模型、损失、算法和组件
│   ├── pipelines/   # 完整实验流程
│   └── cli/         # 命令行、配置读取与pipeline分发
├── configs/         # 实验配置文件（独立于src）
├── data/            # MNIST及其他简单图像数据（独立于src）
├── outputs/         # 所有实验输出（独立于src）
└── main.py          # 统一项目入口（独立于src）
```

不得再单独创建`base/`。原本属于base的随机种子、设备、配置对象、公共类型和数学约定，统一放在`src/utils/`。

## 数据集与数据接口

项目应支持用于学习和验证的简单数据集，至少包括MNIST，并允许扩展到Fashion-MNIST、CIFAR-10子集、scikit-learn二维分布、用户提供的PNG/JPG图像文件夹等。数据文件统一放在根目录`data/`，不得把数据文件硬编码到`src/`内部。

数据相关代码按职责拆分：

- `src/utils/`：通用数据加载、预处理、归一化、随机划分和可视化辅助；
- `src/modules/`：必要的数据编码器、简单图像模型或latent组件；
- `src/pipelines/`：具体数据集实验流程，例如`mnist_flow`、`simple_image_flow`；
- `configs/`：数据集名称、路径、图像大小、通道数、batch size等配置。

首次运行应支持自动下载公开数据集；若网络不可用，应给出清晰提示。数据下载缓存、用户数据和实验输出必须分开。
## 各层职责

### `src/utils/`

放置稳定且跨实验复用的公共能力：随机种子、设备选择、配置辅助、数据类型、采样器、ODE/Euler积分、Sinkhorn工具、指标、绘图、动画、热力图、输出目录和实验元数据保存。不得依赖具体pipeline的业务逻辑。

### `src/modules/`

放置可独立导入和测试的组件：速度场网络、时间/条件嵌入、Flow Matching损失、路径构造、随机或OT耦合、简单编码器/解码器、评价器接口等。模块不负责解析命令行，不负责创建最终实验目录。

### `src/pipelines/`

一个pipeline对应一个完整实验，例如`unconditional_2d_flow`、`conditional_2d_flow`、`ot_flow`、`simple_image_flow`。每个pipeline负责读取配置、构造数据和模型、训练、采样、评估并调用公共可视化工具。

### `src/cli/`

负责命令行参数、配置文件读取、pipeline注册和启动分发。关键参数不得硬编码在pipeline中。

## 统一启动方式

根目录必须保留统一入口：

```powershell
python main.py --config configs/conditional_2d.yaml
python main.py --pipeline ot_flow --config configs/ot_flow.yaml
```

配置至少应控制pipeline名称、随机种子、设备、训练步数、batch size、学习率、模型规模、ODE步数、数据规模、OT参数、输出目录和动画开关。运行开始时打印最终生效配置；命令行参数可覆盖配置文件。

## 新增学习模块的要求

每新增一个学习模块，应在`src/utils`、`src/modules`、`src/pipelines`、`src/cli`四个层级中补充必要代码，并增加至少一个`configs/`配置和统一`main.py`启动方式。若某一层确实不需要新增代码，应在README或实验说明中明确原因。新模块不得复制粘贴已有训练、采样和绘图逻辑，应优先复用公共实现。

## 输出与可视化规范

所有实验输出统一放在根目录`outputs/`，每个pipeline使用独立目录，不能覆盖既有结果：

```text
outputs/
└── conditional_2d_flow/
    └── 20260829_153000_seed42/
```

每个pipeline至少生成：

- `overview.png`：核心结果总览；
- `loss_curve.png`：训练损失及平滑曲线；
- `trajectory.png`或等价采样过程图；
- `velocity_field*.png`：关键时刻速度场；
- `metrics.json`：指标；
- `config_resolved.yaml`：实际配置；
- 训练日志或简要实验元数据。

根据实验性质，尽量补充不同时间状态、条件对照、同噪声不同条件轨迹、速度场差分、OT配对、运输距离、失败样本和动画GIF。学习项目的可视化应优先帮助理解原理，而不是只展示最终效果。

## 实验说明文档

代码必须输出图片、指标、配置和日志，使实验结果可被解释。完整的图像说明文档可以由Codex根据这些输出在实验完成后生成，不要求每个pipeline实现报告生成器。若用户要求报告，报告应逐图解释图像含义、坐标和颜色、对应公式、预期现象、实际结果、误差来源及与下一实验的关系。

## 代码质量与复现

训练、采样、评估和绘图分离；函数和类保持小而清晰；固定种子后尽量可复现；运行时打印设备、配置和版本；修改后至少执行语法检查和小规模smoke test；不删除或覆盖用户已有文件；优先兼容当前PyTorch环境。

## 当前实验演进

```text
无条件二维速度场
→ 条件二维速度场
→ OT耦合Flow Matching
→ 简单图像或latent生成
→ latent/Decoder/MMDiT瓶颈诊断
→ 科学图像生成与SIQA/SIU2A
```

每次实验只引入一个主要变量，并保留清晰baseline，说明新增变量改变了什么。



## 代码与文档规范补充

- Python代码必须保持正常的多行结构，禁止为了压缩篇幅将多个语句、循环或条件大量写在同一行；
- HTML、CSS和JavaScript必须保持可读的缩进和分行结构，禁止将整个页面或主要逻辑压缩成单行；
- 新增代码应优先使用清晰的函数、类和模块，避免超长函数以及训练、采样、评估、绘图全部混杂；
- 每个pipeline的结果必须写入根目录`outputs/<pipeline>/`，不得在根目录创建`outputs_<pipeline>`形式的平行结果目录；
- 不同pipeline之间必须使用独立输出子目录，实验结果不得互相覆盖；
- 生成的图片、动画、loss曲线、速度场、轨迹、指标和配置应使用有顺序、有意义的文件名；
- GitHub Pages相关HTML统一放在`docs/`，并提供可直接访问的`docs/index.html`；
- 交互式教程应优先服务于新手理解，至少解释时间`t`、粒子/样本、速度场、条件和ODE更新的含义；
- 修改代码后必须执行格式检查、语法检查和小规模smoke test；
- 提交前应检查`git status`，确认没有误提交缓存、虚拟环境、临时文件或重复输出；
- 不应为了追求复杂架构而牺牲学习项目的可读性；任何抽象都必须带来代码复用或概念解释上的实际收益。
