# 科学图像生成模型：Flow Matching基础与瓶颈诊断笔记

> 日期：2026-08-28  
> 目的：记录从二维Flow Matching实验到Qwen-Image科学图像生成研究的基础理解、研究猜想与后续验证问题。

## 1. 当前研究目标

希望利用现有科学图像数据、科学图像质量评价器SIQA，以及科学图像找错纠错评测框架SIU2A，研究如何提升现有生图模型的科学图像生成质量。现阶段不优先进行百万规模数据采集，也不急于重新设计完整生成架构，而是先定位现有模型的能力瓶颈，再针对瓶颈进行微调。

核心问题是：

> 科学图像生成失败，究竟来自语义条件理解不足、MMDiT生成的latent不足，还是VAE编码与解码造成的信息损失？模型内部是否存在可区分的“科学正确—科学错误”语义表示？

## 2. Flow Matching基础理解

Flow Matching学习的是一个随时间变化的速度场：

$$
v_\theta(x_t,t,c)
$$

其中，$x_t$是当前生成状态，$t$是生成时间，$c$是文本条件。在线性条件路径下：

$$
x_t=(1-t)x_{noise}+t x_{data}
$$

监督速度为：

$$
u_t=x_{data}-x_{noise}
$$

模型通过速度回归学习：

$$
v_\theta(x_t,t,c)\approx u_t
$$

推理时从高斯噪声开始，通过ODE逐步更新：

$$
x_{t+\Delta t}=x_t+\Delta t\,v_\theta(x_t,t,c)
$$

二维实验中的一个粒子代表一个完整样本，而不是样本中的一个局部点。映射到生图模型时：

| 二维Flow实验 | 高维图像生成 |
|---|---|
| 一个二维粒子 | 一整张图像的latent |
| 二维坐标 | latent的全部通道和空间位置 |
| 二维速度箭头 | 整张latent的联合变化速度 |
| 双月牙点云 | 大量真实图像形成的数据分布 |
| MLP | MMDiT/DiT |
| 粒子ODE轨迹 | 图像latent的生成轨迹 |

因此，图像生成不是大量二维粒子组成一张图，而是“一整张噪声图像对应高维空间中的一个点”，该点沿条件速度场移动，最终成为一张图像的latent。

## 3. Qwen-Image的简化结构

Qwen-Image可以简化为：

```text
Prompt
  ↓
多模态语义编码器
  ↓
文本语义Token c ─────────────────┐
                                 ↓
噪声latent z_t → MMDiT → 速度vθ(z_t,t,c)
                                 ↓
                            Flow ODE积分
                                 ↓
                            最终图像latent
                                 ↓
                            VAE Decoder
                                 ↓
                              RGB图像
```

各模块的主要作用：

- **语义编码器**：理解对象、属性、数量、存在性、方向和科学关系；
- **MMDiT**：让文本Token与图像Token交互，并预测当前图像latent的高维速度；
- **Flow Matching**：规定从噪声到数据的训练路径和速度学习目标；
- **VAE Encoder/Decoder**：在RGB图像与压缩latent之间转换。

MMDiT最终输出的不是RGB图像，而是与当前图像latent形状一致的速度张量。经过多次速度预测和ODE更新后，最终latent再由VAE Decoder还原为RGB图像。

## 4. 当前形成的主要猜想

### 猜想A：科学图像问题可能来自VAE表示或解码瓶颈

科学图像包含大量高频、细粒度且具有离散意义的结构，例如：

- 小字号文字和上下标；
- 箭头尖端与方向；
- 化学键、电路线和细线；
- 坐标刻度、虚线与实线；
- 公式符号和标签绑定。

这些信息可能在VAE Encoder压缩时丢失，也可能在Decoder重建时被模糊或错误还原。因此不能把所有错误都归因于MMDiT。

### 猜想B：科学语义可能没有被充分编码

文本编码器可能能够区分字面不同的Prompt，但不一定理解科学关系。例如它可能区分“向上”和“向下”，却不知道在具体物理情境下哪一个方向正确。

需要区分两个能力：

1. **Prompt敏感性**：模型能否察觉Prompt发生了变化；
2. **科学正确性**：模型能否判断哪一种变化符合科学规律。

### 猜想C：MMDiT可能理解了Prompt，但没有形成正确生成轨迹

即使文本编码器能够表示科学语义，MMDiT也可能没有把它正确转换为图像latent的速度变化，表现为：

- 对象缺失或多余；
- 数量错误；
- 箭头方向错误；
- 空间关系或拓扑错误；
- 图像与Prompt不一致。

### 猜想D：正确与错误语义可能在内部表示中可分

对于只改变一个科学属性的成对Prompt，例如：

```text
正确：重力方向竖直向下
错误：重力方向竖直向上
```

固定相同随机种子后，可以比较：

- 文本编码器输出；
- MMDiT不同层的文本Token和图像Token；
- 不同时间步的生成latent；
- 预测的clean latent；
- 最终解码图像。

但“两个表示不同”只能证明模型感知了字面或语义变化，不能证明模型知道哪一个科学上正确。科学正确性必须通过跨场景泛化、SIQA/SIU2A评价或独立探针进一步验证。

## 5. 建议的瓶颈诊断实验

### 5.1 VAE重建上限实验

对科学正确的真实图像执行：

$$
I\rightarrow E_{VAE}(I)\rightarrow D_{VAE}(E_{VAE}(I))=\hat I
$$

比较原图和重建图的SIQA分数及SIU2A错误：

$$
\Delta_{VAE}=SIQA(I)-SIQA(\hat I)
$$

如果重建后已经出现文字、箭头或细线错误，说明VAE是明确瓶颈。这个实验首先检查整个Encoder—Decoder通道；只有确认Encoder保留了信息后，才能进一步把问题归因于Decoder。

### 5.2 MMDiT生成能力实验

使用同一个Decoder分别解码真实latent和生成latent：

$$
\hat I_{real}=D(E(I))
$$

$$
\hat I_{gen}=D(z_{gen})
$$

如果$\hat I_{real}$质量好，而$\hat I_{gen}$存在科学结构错误，那么主要瓶颈更可能在文本条件或MMDiT生成轨迹。

### 5.3 最小差异Prompt实验

构造只改变一个属性的Prompt对：

- 存在/不存在；
- 向上/向下；
- 串联/并联；
- 凸透镜/凹透镜；
- 正确数量/错误数量；
- 正确科学关系/错误科学关系。

固定同一个初始噪声，得到：

$$
z_t^+=F(z_0,P^+,t),\qquad z_t^-=F(z_0,P^-,t)
$$

分析差分：

$$
\Delta z_t=z_t^+-z_t^-
$$

观察差异何时出现、是否持续存在，以及是否集中在应当改变的图像区域。

### 5.4 中间表示与时间轨迹实验

Flow Matching下，可以根据当前latent和预测速度估计最终clean latent：

$$
\hat z_{clean}(t)=z_t+(1-t)v_\theta(z_t,t,c)
$$

再进行解码：

$$
\hat I_{clean}(t)=D(\hat z_{clean}(t))
$$

逐时间观察：

- 全局布局何时确定；
- 科学对象何时出现；
- 错误关系何时形成；
- 错误是否在后续被修正；
- SIQA分数如何随时间变化。

PCA、t-SNE等降维方法适合观察分布和轨迹，但不能单独证明科学正确性。更可靠的组合是：

```text
latent降维轨迹
+ 预测clean latent解码
+ 空间Token差异热力图
+ SIQA/SIU2A评价
+ 线性探针与跨场景测试
```

## 6. 可能的训练与改进位置

根据诊断结果，主要有两个直接可调整模块：

### 调整MMDiT

适合处理：

- 科学对象、数量和关系错误；
- Prompt遵循不足；
- 图像拓扑和布局错误；
- 正确与错误语义没有进入生成轨迹。

可以利用科学图文数据、SIQA候选排序和正确—错误对进行LoRA或偏好微调。

### 调整VAE Decoder

适合处理：

- 文字模糊；
- 细线断裂；
- 箭头尖端丢失；
- 上下标和公式细节损坏；
- 正确latent无法被清晰重建。

可以冻结Encoder，以科学图像为训练数据，针对Decoder进行重建与感知质量适配。但若Encoder已经丢失关键信息，只训练Decoder无法完全恢复。

## 7. 当前研究主线

现阶段可以把研究概括为：

> 利用SIQA和SIU2A对科学图像生成模型进行模块级瓶颈诊断，分析科学图像质量不足来自文本语义表示、MMDiT条件生成还是VAE表示与解码；进一步研究正确与错误科学语义在模型内部是否可区分，并根据诊断结果实施质量感知微调。

相比直接扩大数据规模，这一路线首先回答三个基础问题：

1. 现有模型是否已经具备科学图像生成潜力？
2. 这种潜力被哪个模块限制？
3. SIQA/SIU2A能否把质量评价转化为有效的诊断和训练信号？

## 8. 当前尚未确定的问题

- 分析对象优先选择文本编码器输出，还是MMDiT中间图像Token？
- SIQA对VAE重建错误和科学语义错误的敏感度是否一致？
- SIU2A中的正确—错误样本能否转换成最小差异Prompt与图像对？
- 正确—错误差分方向能否跨对象、跨Prompt表述和跨学科泛化？
- 科学图像的主要损失发生在Encoder压缩、Decoder重建还是MMDiT生成阶段？

这些问题将决定后续训练重点是MMDiT、Decoder，还是科学语义条件表示。

